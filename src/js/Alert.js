//Create an Alert.js file (module). 
//In the file add a class called Alert and export it as default.
export default class Alert {


    constructor(alertMessage) {
        this.alertMessage = alertMessage
    }
}